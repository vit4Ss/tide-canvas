# 构建上下文裁剪：密钥/版本库/日志/SQL 数据 等不进镜像

# 密钥与本地配置（运行时用 --env-file 注入，绝不打进镜像）
.env
.env.example
configs/config.yaml

# 版本库与构建产物
.git
.gitignore
*.exe
/bin/
/dist/

# 日志与临时
*.log
logs/
tmp/

# 镜像内用不到的文档与建库 SQL（建表在宿主机操作，走 git pull 的 sql/）
docs/
README.md
sql/
