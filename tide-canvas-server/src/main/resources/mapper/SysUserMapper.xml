<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tidecanvas.mapper.SysUserMapper">

    <resultMap id="BaseResultMap" type="com.tidecanvas.model.entity.SysUserDO">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar" property="avatar"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="api_quota" property="apiQuota"/>
        <result column="storage_quota" property="storageQuota"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, email, phone, password, nickname, avatar, role, status,
        api_quota, storage_quota, last_login_time, create_time, update_time, deleted
    </sql>

    <!-- 今日新增用户数 -->
    <select id="countTodayNew" resultType="java.lang.Long">
        SELECT COUNT(*) FROM sys_user
        WHERE deleted = 0 AND DATE(create_time) = CURDATE()
    </select>

    <!-- 悲观锁查询：SELECT ... FOR UPDATE -->
    <select id="selectForUpdate" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_user
        WHERE id = #{id} AND deleted = 0
        FOR UPDATE
    </select>

    <!-- 今日活跃用户数（今天登录过的用户） -->
    <select id="countTodayActive" resultType="java.lang.Long">
        SELECT COUNT(*) FROM sys_user
        WHERE deleted = 0 AND status = 1 AND DATE(last_login_time) = CURDATE()
    </select>

    <!-- 按日期范围统计用户增长 -->
    <select id="countByDateRange" resultType="java.util.Map">
        SELECT DATE(create_time) AS date, COUNT(*) AS count
        FROM sys_user
        WHERE deleted = 0
          AND create_time BETWEEN #{startDate} AND #{endDate}
        GROUP BY DATE(create_time)
        ORDER BY date
    </select>

</mapper>
