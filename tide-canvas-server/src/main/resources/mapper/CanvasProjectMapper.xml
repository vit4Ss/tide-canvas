<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tidecanvas.mapper.CanvasProjectMapper">

    <resultMap id="BaseResultMap" type="com.tidecanvas.model.entity.CanvasProjectDO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="thumbnail" property="thumbnail"/>
        <result column="canvas_data" property="canvasData"/>
        <result column="is_public" property="isPublic"/>
        <result column="share_token" property="shareToken"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, name, description, thumbnail, is_public, share_token,
        status, create_time, update_time, deleted
    </sql>

    <!-- 按分享Token查询(不含canvasData大字段) -->
    <select id="selectByShareToken" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM canvas_project
        WHERE deleted = 0 AND share_token = #{shareToken}
    </select>

    <!-- 查询用户项目数 -->
    <select id="countByUserId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM canvas_project
        WHERE deleted = 0 AND user_id = #{userId}
    </select>

    <!-- 按日期统计创作量 -->
    <select id="countByDateRange" resultType="java.util.Map">
        SELECT DATE(create_time) AS date, COUNT(*) AS count
        FROM canvas_project
        WHERE deleted = 0
          AND create_time BETWEEN #{startDate} AND #{endDate}
        GROUP BY DATE(create_time)
        ORDER BY date
    </select>

</mapper>
