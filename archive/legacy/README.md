# Legacy Archive

Archived on 2026-06-29.

The following project trees are no longer part of the active development surface:

- `tide-canvas-web`: standalone Next.js application.
- `tide-canvas-server`: Java/Spring Boot backend.
- `docker-compose.java.yml`: legacy Java-stack Docker Compose entrypoint.

Current active project areas are expected to be:

- `tide-canvas-frontend`
- `tide-canvas-go`

Only Git-tracked files were moved here. Local build outputs, dependency folders, and local-only config files were intentionally left out.
