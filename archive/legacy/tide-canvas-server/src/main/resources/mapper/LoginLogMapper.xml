<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tidecanvas.mapper.LoginLogMapper">

    <!-- 今日成功登录次数 -->
    <select id="countTodayLogins" resultType="java.lang.Long">
        SELECT COUNT(*) FROM login_log WHERE status = 1 AND DATE(create_time) = CURDATE()
    </select>

    <!-- 每日成功登录次数 -->
    <select id="loginByDateRange" resultType="java.util.Map">
        SELECT DATE(create_time) AS date, COUNT(*) AS count
        FROM login_log
        WHERE status = 1 AND create_time BETWEEN #{startDate} AND #{endDate}
        GROUP BY DATE(create_time)
        ORDER BY date
    </select>

    <!-- 清理过期登录日志 -->
    <delete id="deleteBefore">
        DELETE FROM login_log WHERE create_time &lt; #{before}
    </delete>

</mapper>
